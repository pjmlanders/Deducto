# Backend API base URL (required for production / Vercel).
# In development, leave empty to use the same origin (e.g. http://localhost:5173).
# On Vercel: set to your deployed server URL (e.g. https://your-app.railway.app).
# Name must be exactly VITE_API_URL (all caps). After adding or changing, trigger a new deploy.
VITE_API_URL=

# Clerk auth (optional in dev — app runs without key using mock user).
# For production, set your Clerk publishable key.
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_...
